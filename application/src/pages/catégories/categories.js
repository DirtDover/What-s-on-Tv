import React from 'react';
import Header from '../../components/Header';

const categories = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default categories;